import Vue from 'vue';
import Vuex from 'vuex';
require('dotenv').config();

Vue.use(Vuex);

export default new Vuex.Store({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
    }
});
